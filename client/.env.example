# Base URL for API calls. Leave empty to use the Vite dev proxy (recommended in dev).
# In production set this to your deployed API origin, e.g. https://api.yourstudio.com
VITE_API_BASE=""

# Where `vite dev` proxies /api to. Only used in development.
VITE_API_PROXY="http://localhost:4000"
